/**
 * Custom MDX components for blog posts
 * These components can be used to override default MDX rendering
 * or add custom interactive components to blog posts
 */

export const BlogMDXComponents = {
  // Add custom components here as needed
  // Example:
  // Callout: ({ children, type }: { children: React.ReactNode; type?: string }) => (
  //   <div className={`callout callout-${type || 'info'}`}>
  //     {children}
  //   </div>
  // ),
}
